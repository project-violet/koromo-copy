<UserControl x:Class="Koromo_Copy_UX3.CommentElements"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Margin="0 0 0 20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBox BorderThickness="0"  x:Name="Author" FontSize="20" FontWeight="Heavy" Text="{Binding Author}" HorizontalAlignment="Left" Grid.Row="0" Grid.Column="0"/>
        <TextBox BorderThickness="0" x:Name="Date" FontSize="10" FontWeight="Heavy" Text="{Binding Date}" HorizontalAlignment="Left" Grid.Row="0" Grid.Column="1"/>
        <TextBox BorderThickness="0" x:Name="Content" TextWrapping="Wrap" FontSize="15" Text="{Binding Content}" HorizontalAlignment="Left" Grid.Row="1" Grid.ColumnSpan="2"/>
    </Grid>
</UserControl>
